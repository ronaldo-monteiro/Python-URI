# Leitura das notas A, B e C
A = float(input(""))
B = float(input(""))
C = float(input(""))

media = (A * 2 + B * 3 + C * 5) / (2 + 3 + 5)
    
    # Exibição do resultado
print(f"MEDIA = {media:.1f}")
